# 建模流程说明

本文档用适合初学者的语言解释数学建模的基本流程和核心概念。

## 为什么不能一上来就用复杂模型

很多人拿到数据就想用神经网络、深度学习，这通常不是好主意。原因：

1. **你还不了解数据**：复杂模型是黑箱，如果数据有问题，你发现不了。
2. **没有比较基准**：不知道简单方法能做到什么程度，就无法判断复杂方法是否真的更好。
3. **调试困难**：复杂模型出错时，你不知道是数据问题、特征问题还是模型问题。

**正确顺序**：先用最简单的方法建立基准（baseline），然后逐步尝试更复杂的方法，每次只改一个变量。

## 什么是 Baseline（基准模型）

Baseline 是一个简单、容易理解的预测方法，用来衡量后续复杂模型的价值。

常见的 baseline：
- **朴素预测**：用上一期的值作为下一期的预测（"明天销量 = 今天销量"）
- **移动平均**：用最近 N 期的平均值作为预测
- **季节性朴素**：用去年同期的值作为预测

如果你的复杂模型比 baseline 还差，说明模型有问题，或者数据不适合用这个方法。

## 什么是特征（Feature）

特征是模型用来做预测的输入变量。

原始数据中的列可能是特征，但更多时候需要我们自己构造：
- **原始特征**：商品类别、价格、日期
- **构造特征**：最近 7 天平均销量、是否节假日、星期几、月份

好的特征比好的模型更重要。这就是"特征工程"。

## 什么是训练集 / 验证集

把数据分成两部分：
- **训练集**：模型用来学习的数据（通常占 70-80%）
- **验证集**：模型没见过的数据，用来评估模型效果（通常占 20-30%）

**为什么需要分**：如果用训练数据评估模型，模型只要"记住"所有数据就能得满分，但遇到新数据就不行了。这叫**过拟合**。

时间序列数据的特殊性：不能随机分割，必须按时间顺序分。用过去的数据训练，用未来的数据验证。

## 什么是过拟合（Overfitting）

过拟合 = 模型把训练数据中的噪声也学进去了。

表现：训练集上效果很好，验证集上效果很差。

类比：一个学生把练习题答案全背下来了，但考试题稍有变化就不会做。

如何避免：
- 用更简单的模型
- 减少特征数量
- 增加训练数据
- 使用正则化（让模型不要过于复杂）

## 如何评价预测效果

常用指标：

| 指标 | 公式 | 含义 |
|------|------|------|
| MAE | $\frac{1}{n}\sum\|y_i - \hat{y}_i\|$ | 平均绝对误差，直观理解"平均差多少" |
| RMSE | $\sqrt{\frac{1}{n}\sum(y_i - \hat{y}_i)^2}$ | 均方根误差，对大误差更敏感 |
| MAPE | $\frac{1}{n}\sum\frac{\|y_i - \hat{y}_i\|}{y_i}$ | 平均绝对百分比误差，直观理解"平均差百分之几" |

选择哪个指标取决于题目要求和业务场景。

## 如何把模型结果写进论文

论文中的模型部分应该包含：

1. **为什么选这个模型**：直觉解释，不要上来就写公式
2. **模型的数学描述**：用 LaTeX 写公式，解释每个符号
3. **如何求解**：用了什么算法、什么库
4. **结果展示**：图表 + 数值
5. **结果解释**：这些数字意味着什么

**常见错误**：
- 只写公式不解释
- 只展示结果不分析
- 结果不好却说"效果很好"
- 图表没有标题和标签

---

**Created:** 2026-05-02
