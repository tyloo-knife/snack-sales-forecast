# 模型假设 - 休闲零食销量预测

## 假设 1：数据真实性

**假设内容**：题目提供的历史销售数据真实反映了实际销售情况。

**为什么需要**：如果数据本身有系统性错误（如录入错误、统计口径不一致），任何模型都无法得到可靠结果。

**可能带来的误差**：如果部分数据存在录入错误，模型可能学到错误的规律。需要通过数据清洗和异常值检测来缓解。

## 假设 2：销售规律稳定性

**假设内容**：过去观察到的销售规律（如季节性、周期性）在未来一段时间内仍然成立。

**为什么需要**：预测的本质是"用过去推断未来"，这假设了规律的延续性。

**可能带来的误差**：如果市场环境发生重大变化（如新竞争对手进入、消费习惯改变），历史规律可能失效。模型无法预测"黑天鹅"事件。

## 假设 3：促销影响可观测

**假设内容**：促销活动对销量的影响可以从数据中观察到，且影响模式相对稳定。

**为什么需要**：促销是影响销量的重要因素，如果无法从数据中识别促销效应，模型会遗漏重要信息。

**可能带来的误差**：不同类型的促销效果差异大，如果数据中没有区分促销类型，可能产生平均化误差。

## 假设 4：天气 / 节假日影响可量化

**假设内容**：天气和节假日对零食销量的影响可以通过特征工程量化。

**为什么需要**：这些是常见的外部影响因素，纳入模型可以提高预测精度。

**可能带来的误差**：天气数据可能不精确（如只记录了城市级别），节假日效应可能逐年变化。

## 假设 5：异常值处理

**假设内容**：数据中的异常值（如极端高销量）是由特殊事件（如促销、节假日）造成的，而非数据错误。

**为什么需要**：异常值处理方式会影响模型效果。如果把正常的促销高峰当作异常值删除，会丢失重要信息。

**可能带来的误差**：如果某些异常值确实是数据错误，保留它们会干扰模型学习。需要结合业务背景判断。

## 假设 6：缺失值处理

**假设内容**：数据缺失是随机发生的，或者缺失原因可以从其他字段推断。

**为什么需要**：如果缺失不是随机的（如某类商品系统性不记录），简单删除或填充会引入偏差。

**可能带来的误差**：不当的缺失值处理可能导致样本偏差，影响模型对特定商品类别的预测能力。

---

**Created:** 2026-05-02
